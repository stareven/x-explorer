//! Watches for connected iOS/Android devices and emits device-list events to the frontend.
//!
//! STUB: This module is a placeholder created in Task 1 so the project compiles
//! standalone. The real implementation is added in Task 5.

use tauri::AppHandle;

pub fn start(_app: AppHandle) {
    // TODO(Task 5): poll/watch for device connect/disconnect events and emit
    // "devices-changed" (or similar) events to the frontend.
}
